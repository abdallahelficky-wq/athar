<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- لازمة فقط على أندرويد 9 وأقدم (28-) لكتابة مجلد التنزيلات العام — 29+ يستخدم MediaStore
         دون أي صلاحية إطلاقاً (راجع MainActivity.writeToDownloads). -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- رؤية حزمة خدمة طابعة Sunmi المدمجة (woyou.aidlservice.jiuiv5) على أندرويد 11+ — بدون هذا
         التصريح قد يفشل bindService/queryIntentServices صامتاً بسبب قيود رؤية الحزم الجديدة
         (package visibility). راجع تقرير الميزة (Step 1-ب) لمصدر اسم الحزمة/الإجراء. -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.AtharPos"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

    </application>

</manifest>
